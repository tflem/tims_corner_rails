<% provide(:title, 'Sign Up') %>
<% provide(:button_text, 'Create My Account') %>

        <!-- Sign Up Section -->
    <section id="sign up" class="container content-section">
        <div class="row">
            <div class="col-md-6 col-md-offset-3">
              <%= form_for(@user) do |f| %>
              <h1 class="text-center">Sign Up</h1>
                <%= render "form", f:f %>
              <% end %>
            </div>
        </div>
    </section>